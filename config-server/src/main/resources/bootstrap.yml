spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nomad-coder/handson
          search-paths:
            - configs
server:
  port: 8888
